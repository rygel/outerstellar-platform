@import io.github.rygel.outerstellar.platform.web.AuthResultFragment
@param model: AuthResultFragment

<div class="card ${model.toneClass}">
    <h3>${model.title}</h3>
    <p>${model.message}</p>
</div>
