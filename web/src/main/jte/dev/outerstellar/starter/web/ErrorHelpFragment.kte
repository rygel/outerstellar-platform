@import dev.outerstellar.starter.web.ErrorHelpFragment
@param model: ErrorHelpFragment

<div>
    <h3>${model.title}</h3>
    <ul class="bullet-list">
        @for (item in model.items)
            <li>${item}</li>
        @endfor
    </ul>
</div>
