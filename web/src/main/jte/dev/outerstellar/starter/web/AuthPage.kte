@import dev.outerstellar.starter.web.AuthPage
@param model: AuthPage

@template.dev.outerstellar.starter.web.Layout(shell = model.shell, content = @`
    <section class="panel hero-grid">
        <div>
            <p class="status-badge">${model.shell.pageTitle}</p>
            <h1>${model.heading}</h1>
            <p>${model.intro}</p>
        </div>
        <div class="panel panel-muted">
            <strong>HTMX</strong>
            <p>${model.helperText}</p>
        </div>
    </section>
    <section class="panel">
        <div class="tabs">
            @for (tab in model.tabs)
                <button class="chip"
                        hx-get="${tab.url}"
                        hx-target="#auth-form-slot"
                        hx-swap="innerHTML">
                    ${tab.label}
                </button>
            @endfor
        </div>
        <div id="auth-form-slot"
             class="panel panel-muted"
             hx-get="${model.defaultFormUrl}"
             hx-trigger="load"
             hx-swap="innerHTML">
        </div>
        <div id="auth-result-slot" class="panel panel-muted"></div>
    </section>
`)
