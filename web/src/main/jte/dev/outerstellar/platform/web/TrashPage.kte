@import dev.outerstellar.platform.web.TrashPage
@import dev.outerstellar.platform.web.Page
@param model: Page<TrashPage>

@template.dev.outerstellar.platform.web.Layout(shell = model.shell, content = @`
    <section class="hero-grid">
        <div class="panel">
            <h1>${model.data.title}</h1>
            <p class="text-muted">${model.data.description}</p>
        </div>
    </section>

    @template.dev.outerstellar.platform.web.components.MessageList(model = model.data.messageList)
`)
